# JSON Data API

The Claude Marketplace Registry publishes its entire catalog as free, static JSON. There is no REST server, no authentication, and no rate limiting beyond GitHub Pages' CDN (responses are cached ~10 minutes). Every file is regenerated by the daily scan and gated by a data validator before it ships.

## Base URL

```
https://shrwnsan.github.io/claude-marketplace-registry/data
```

CORS is enabled for every origin, so you can fetch these files directly from browser apps, scripts, or agents. No key is ever required.

## Endpoints

| File | Contents |
|------|----------|
| `stats.json` | Ecosystem overview, growth rates, daily trend snapshots, topic counts, insights, quality indicators |
| `marketplaces.json` | Every discovered marketplace (bare array) |
| `plugins.json` | The full plugin catalog (bare array) |
| `history.json` | Daily snapshot history per metric |

### stats.json

```json
{
  "success": true,
  "data": {
    "overview": {
      "totalPlugins": 3251,
      "totalMarketplaces": 250,
      "totalDevelopers": 410,
      "totalStars": 642501,
      "lastUpdated": "2026-09-18T01:12:00Z",
      "growthRate": { "plugins": 155.2, "marketplaces": 150.0, "developers": 3.5, "stars": 106.2 }
    },
    "plugins":  [{ "date": "2026-09-17", "value": 1274, "change": 1274 }],
    "marketplaces": [{ "date": "2026-09-17", "value": 100, "change": 100 }],
    "developers":  [{ "date": "2026-09-17", "value": 396, "change": 396 }],
    "stars":       [{ "date": "2026-09-17", "value": 311529, "change": 311529 }],
    "categories": [{ "id": "mcp", "name": "mcp", "count": 875, "description": "…" }],
    "insights": ["…"],
    "manifestCoverage": { "withManifest": 184, "total": 250, "rate": 73.6 },
    "maintenance": { "recentlyUpdated": 206, "recentlyUpdatedRate": 82.4, "staleOver180Days": 0 },
    "avgStarsPerMarketplace": 2570
  },
  "meta": { "timestamp": "2026-09-18T01:12:00Z" }
}
```

### marketplaces.json / plugins.json

Both are bare arrays. `marketplaces.json` entries carry `id`, `name`, `description`, `url`, `stars`, `forks`, `language`, `license`, `topics[]`, `hasManifest`, `createdAt`, `updatedAt`. `plugins.json` entries carry `id`, `name`, `description`, `author`, `version`, `skills[]`, `marketplaceId`, `marketplaceName`, and source metadata.

### Joining plugins to marketplaces

```js
String(plugin.metadata.marketplaceId) === String(marketplace.id)
```

Compare as strings on both sides — IDs are numeric-looking but join as strings.

## Data contract notes

- `growthRate` compares the current scan against the oldest snapshot within the last 30 days. Treat `null` (early history) as "no data", not zero.
- Trend arrays gain one point per daily scan; the site renders big-number deltas until five snapshots exist.
- Topic counts overlap: one plugin appears under every topic its parent marketplace carries. Never render them as a pie chart.
- Plugin-level `stars` are the parent marketplace's stars. Download counts are not published — GitHub does not expose them per plugin, and none of these numbers are estimated or mocked.

## Examples

```bash
# Ecosystem overview
curl -s https://shrwnsan.github.io/claude-marketplace-registry/data/stats.json | jq '.data.overview'

# Top 5 marketplaces by stars
curl -s https://shrwnsan.github.io/claude-marketplace-registry/data/marketplaces.json | \
  jq 'sort_by(-.stars)[:5] | .[] | {name, stars}'

# All plugins by one author
curl -s https://shrwnsan.github.io/claude-marketplace-registry/data/plugins.json | \
  jq '[.[] | select(.author == "anthropics")] | length'
```

## Freshness & guarantees

The daily scan (00:00 UTC) runs 11 discovery strategies, validates every manifest, and opens a data-update PR that a validator gates on shape, cross-file consistency, and freshness before auto-merge. Catalog data on the CDN is at most one day old; deleted repositories are pruned and the registry is monotonic — everything ever discovered stays indexed unless it disappears from GitHub.

Interactive browsing of the same data lives on the site itself; a human-readable version of this reference is at [/docs/api](https://shrwnsan.github.io/claude-marketplace-registry/docs/api).
